server:
  port: 8000
spring:
  application:
    name: EHR-SYSTEM
  datasource:
    url: jdbc:postgresql://localhost:5432/ehr_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:  
      ddl-auto: update
    show-sql: true
  
